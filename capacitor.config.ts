import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'uk.softwarestudio.capfirestoretest',
  appName: 'Capacitor Firestore Test',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
